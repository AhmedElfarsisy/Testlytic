<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elfarsisy.lovetypetest">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_yin_yang"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_yin_yang"
        android:supportsRtl="true"
        android:theme="@style/fullScreen">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>
        <activity android:name=".ui.activities.HomeActivity"
            android:configChanges="keyboard|keyboardHidden|layoutDirection|orientation|screenSize|navigation"></activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:configChanges="keyboard|keyboardHidden|layoutDirection|orientation|screenSize|navigation"
            android:theme="@style/splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>